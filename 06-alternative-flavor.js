const items = ['item1', 'item2'] // the same module.exports.items = ['item1', 'item2']

const person = {
    name: "Bob"
}

module.exports = {items, person} // the same module.exports.singlePerson = person
